<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Background color when unselected -->
    <corners android:radius="20dp" /> <!-- Adjust corner radius as needed -->
    <padding
        android:bottom="8dp"
    android:left="0dp"
    android:right="0dp"
    android:top="0dp" />
</shape>